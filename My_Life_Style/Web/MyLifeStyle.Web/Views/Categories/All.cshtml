@using MyLifeStyle.Web.ViewModels.Categories
@model IEnumerable<AllCategoriesViewModel>

@{
    this.ViewData["Title"] = "Categories";
}

<h1>@this.ViewData["Title"]</h1>

@foreach (var category in Model)
{
<div>@category.Name ( @category.ArticlesCount )</div>
}