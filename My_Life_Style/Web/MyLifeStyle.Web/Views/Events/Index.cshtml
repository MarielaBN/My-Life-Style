@using MyLifeStyle.Web.ViewModels.Events
@model IEnumerable<AllEventsViewModel>

@{
    this.ViewData["Title"] = "Events";
}

<h1>@this.ViewData["Title"]</h1>

@foreach (var oneEvent in Model)
{
    <div>@oneEvent.PublicationTitle, @oneEvent.StartTime, (@oneEvent.CategoryName) </div>
}